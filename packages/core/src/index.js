'use strict'

import * as data from './data'
import * as registry from './registry'
import * as zip from './zip'
import * as log from './log'

export { data, registry, zip, log }

export default {
  data,
  registry,
  zip,
  log,
}
